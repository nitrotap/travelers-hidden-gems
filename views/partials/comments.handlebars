<div class="comments">
{{#each this as |comment|}}
<p class="comment-text">{{comment.comment_text}}</p>
<p class="comment-byline"><span class="comment-user">{{comment.user.username}}</span> | {{format_date comment.created_at}} ({{timeSince comment.created_at}})</p>
{{/each}}
</div>